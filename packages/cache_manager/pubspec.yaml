name: cache_manager
description: A service which caches api calls in memory and persistant cache
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  jsonable:
    path: ../jsonable
  shared_preferences: ^2.3.2

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.8
  very_good_analysis: ^6.0.0
